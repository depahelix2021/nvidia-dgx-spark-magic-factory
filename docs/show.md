# show.sh — Documentation

## ptr f0020001 — Overview

Prints the NVIDIA DGX Spark Magic Factory services header, then delegates to `./svc open` which syncs the service registry with reality and opens all running+ready services in Firefox.
